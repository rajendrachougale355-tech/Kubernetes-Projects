apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-pro-app.fullname" . }}
  labels:
    {{- include "my-pro-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-pro-app.selectorLabels" . | nindent 4 }}
